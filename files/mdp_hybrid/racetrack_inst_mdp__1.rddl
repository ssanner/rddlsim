non-fluents nf_racetrack_1 {
	domain = racetrack_mdp;
	objects {
		poi : {p_center};//, p_left, p_right};
	};
	
	non-fluents {
		MAX-GAS = 2.0;
	};
}

instance racetrack_inst_mdp__1 {
 	domain = racetrack_mdp;
	non-fluents = nf_racetrack_1;
	
	init-state {
		x(p_center) = 5.0; y(p_center) = 1.0;
		//x(p_left) = 4.7; y(p_left) = 1.0;
		//x(p_right) = 2.8; y(p_right) = 0.2;
		//x(p_up) = 2.8; y(p_up) = 0.8;
		//x(p_down) = 0.5; y(p_down) = 0.3;
	};
	
	horizon = 20;
 	discount = 1.0;
}